using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace nilnul.slew.axiomatic.sigmable
{
///Suppose that X is a set and Σ is a σ-algebra over X. Then a measure μ is a function with domain Σ and codomain  (see extended interval) such that the following properties are satisfied:

///Non-negativity, i.e. μ(E) ≥ 0 for all E in Σ; 
///Null empty set, i.e. μ(Ø) = 0 (where Ø denotes the empty set); 
///Countable additivity or σ-additivity: if  is a countable collection of pairwise disjoint sets in Σ, then the union of all Ei has measure equal to the sum of each Ei's measure, i.e.: 
	
	public class Measure
	{


 

	}
}
