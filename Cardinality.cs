using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace nilnul.collective
{
	/// <summary>
	/// after natural number is defined,we finaly can define the cardinality of a set. in set project, we only defines bool contains.
	/// </summary>
	/// <remarks>
	/// for finite collection, this is count;
	/// for infinite, count is not defined; we can only use cardinality;
	/// </remarks>
	/// alias:
	///		measure
	///		lebesgue
	///		length
	///		
	/// vs: beth number.
	/// 
	/// 
	public class ICardinality
    {
    }
}
